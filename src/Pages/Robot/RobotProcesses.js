import Card from "react-bootstrap/Card";

const RobotProcesses = (props) => {

    return (
        <Card className="card">
            <Card.Title className="card-header-first">Processes</Card.Title>

            <Card.Body style={{padding: '0px'}}>

            </Card.Body>
        </Card>
    );

};

export default RobotProcesses;